# SQLite. Versions 3.8.0 and up are supported.
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
default: &default
  adapter: sqlite3
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

# ! How to make this db usable across multiple machines? 
# ! How to upload to heroku?
development:
  adapter: postgresql
  encoding: unicode
  database: supertonic_dev 
  host: localhost
  pool: 5
  username: joey
  password: 525

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: db/test.sqlite3

production:
  adapter: postgresql
  encoding: unicode
  database: supertonic_production
  host: localhost
  pool: 5
  username: joey
  password: 525
